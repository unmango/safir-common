{
  "name": "@safir/protos",
  "scripts": {
    "build": "run-script-os",
    "build:windows": "scripts/build.ps1",
    "build:default": "scripts/build.sh",
    "prepack": "yarn build && mv dist/* .",
    "postpack": "cat assets.txt | xargs rm -r"
  },
  "dependencies": {
    "grpc-web": "^1.2.1"
  },
  "devDependencies": {
    "run-script-os": "^1.1.6"
  }
}